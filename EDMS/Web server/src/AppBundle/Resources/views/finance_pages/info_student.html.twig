{% extends "@App/finance_pages/index.html.twig" %}


 {% block showUser %}
    <li>
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Welcome, {{ user.name }} <span class="caret"></span></a>
        <ul class="dropdown-menu">
            <li class="active">
                {% if user.image is empty %}
                    <img src="{{ asset('assets/person_images/default.png') }}" width="100px" class="img-rounded">
                {% else %}
                    <img src="{{ asset('assets/person_images/'~user.image) }}" width="100px" class="img-rounded">
                {% endif %}
            </li>
            <li role="separator" class="divider"></li>
            <li class="dropdown-header">{{ user.type|upper }}</li>
        </ul>
    </li>
{% endblock %}


{% block navbar %}
    <li class="active"><a href="{{ path('finance') }}">Home</a></li>
    <li><a href="{{ path('show_student')}}">students</a></li>
    <li><a href="{{ path('profile')}}">Profile</a></li>
    <li><a href="{{ path('add')}}">Add Payment</a></li>
{% endblock %}


{% block breadcrumb %}
    <li>/<a href="{{ path('finance') }}">Home</a></li>
    <li class="active">/<a href="{{ path('show_student') }}">showStudent</a></li>
    <li class="active">/<a href="#">student {{ students.name }} {{ students.surname }}</a></li>
{% endblock %}

{% block page_title %}
    <h1><span class="glyphicon glyphicon-cog" aria-hidden="true"></span> Student Finance <small>Dorm Management System</small></h1>
{% endblock %}


{% block body %}



    <h3 class="title-application">{{ students.name }} {{ students.surname }}</h3>

    <div class="panel panel-default">
        <div class="panel-heading main-color-bg">
            <h3 class="panel-title">Payments</h3>
        </div>
        <div class="panel-body">


            <div class="top-button-group">

            <a href="{{ path('add') }}" class="button-style btn btn-success top-button"><i class="glyphicon glyphicon-pencil"></i> Add Payment</a>
            </div>

            <table class="table table-striped table-hover">
                <tr>
                    <th>Date</th>
                    <th>amount</th>


                </tr>
                <tr>
                   {% for row in em %}
                <tr>
                    <td>{{  row.dateP|date('Y-m-d')}}</td>
                    <td>{{  row.amount }}</td>
                </tr>
                    {% endfor %}

                </tr>


                {#{% for row in students %}#}
                    <tr>

                        {#<td>Krist</td>#}
                        {#<td>ZENGINDEMIR</td>#}
                        {#<td>200</td>#}
                        {#<td>{{ row.dateassigned|date('y-M-d')}}</td>#}
                    </tr>
                {#{% endfor %}#}

            </table>
        </div>
    </div>


{% endblock %}
















